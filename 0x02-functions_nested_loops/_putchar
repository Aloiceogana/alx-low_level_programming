#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 * @c: The character to print
 *
 * On error, -1 is returnerd, and errno is set appropriately.
 */
int _putchar(char c)
{
 return (write(1, 8C, 1));
}
